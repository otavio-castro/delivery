# Variáveis de Ambiente - GoDelivery Backend

# Connection String do PostgreSQL
# Usar esta variável para apontar para banco Neon ou outro banco PostgreSQL
# Se não definida, usa a connection string do appsettings.Development.json
CONNECTION_STRING=Host=ep-spring-hill-acib81hy.sa-east-1.aws.neon.tech;Database=neondb;Username=neondb_owner;Password=SUA_SENHA;SSL Mode=Require;Trust Server Certificate=true

# Ou use DATABASE_URL (prioridade maior, formato Render/Heroku)
# DATABASE_URL=postgresql://user:password@host:5432/database

# Porta da aplicação (usado pelo Render, padrão 8080)
# PORT=8080

# Ambiente de execução
# ASPNETCORE_ENVIRONMENT=Production
